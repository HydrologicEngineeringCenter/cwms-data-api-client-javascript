/*
 * CWMS Radar
 * CWMS REST API for Data Retrieval
 *
 * The version of the OpenAPI document: 2.5
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


package org.openapitools.client.model;

import java.util.Objects;
import java.util.Arrays;
import com.google.gson.TypeAdapter;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import java.io.IOException;
import java.util.ArrayList;
import java.util.List;
import org.openapitools.client.model.LocationAlias;
import org.openapitools.client.model.LocationCatalogEntry;
import org.openapitools.client.model.TimeSeriesExtents;
import org.openapitools.client.model.TimeseriesCatalogEntry;

import javax.ws.rs.core.GenericType;

import java.io.IOException;
import java.lang.reflect.Type;
import java.util.logging.Level;
import java.util.logging.Logger;
import java.util.ArrayList;
import java.util.Collections;
import java.util.HashSet;
import java.util.HashMap;
import java.util.Map;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapter;
import com.google.gson.TypeAdapterFactory;
import com.google.gson.reflect.TypeToken;
import com.google.gson.JsonPrimitive;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import com.google.gson.JsonDeserializationContext;
import com.google.gson.JsonDeserializer;
import com.google.gson.JsonSerializationContext;
import com.google.gson.JsonSerializer;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import com.google.gson.JsonParseException;

import org.openapitools.client.JSON;

@javax.annotation.Generated(value = "org.openapitools.codegen.languages.JavaClientCodegen", date = "2023-07-12T11:57:07.491-05:00[America/Chicago]")
public class CatalogEntry extends AbstractOpenApiSchema {
    private static final Logger log = Logger.getLogger(CatalogEntry.class.getName());

    public static class CustomTypeAdapterFactory implements TypeAdapterFactory {
        @SuppressWarnings("unchecked")
        @Override
        public <T> TypeAdapter<T> create(Gson gson, TypeToken<T> type) {
            if (!CatalogEntry.class.isAssignableFrom(type.getRawType())) {
                return null; // this class only serializes 'CatalogEntry' and its subtypes
            }
            final TypeAdapter<JsonElement> elementAdapter = gson.getAdapter(JsonElement.class);
            final TypeAdapter<LocationCatalogEntry> adapterLocationCatalogEntry = gson.getDelegateAdapter(this, TypeToken.get(LocationCatalogEntry.class));
            final TypeAdapter<TimeseriesCatalogEntry> adapterTimeseriesCatalogEntry = gson.getDelegateAdapter(this, TypeToken.get(TimeseriesCatalogEntry.class));

            return (TypeAdapter<T>) new TypeAdapter<CatalogEntry>() {
                @Override
                public void write(JsonWriter out, CatalogEntry value) throws IOException {
                    if (value == null || value.getActualInstance() == null) {
                        elementAdapter.write(out, null);
                        return;
                    }

                    // check if the actual instance is of the type `LocationCatalogEntry`
                    if (value.getActualInstance() instanceof LocationCatalogEntry) {
                        JsonObject obj = adapterLocationCatalogEntry.toJsonTree((LocationCatalogEntry)value.getActualInstance()).getAsJsonObject();
                        elementAdapter.write(out, obj);
                        return;
                    }

                    // check if the actual instance is of the type `TimeseriesCatalogEntry`
                    if (value.getActualInstance() instanceof TimeseriesCatalogEntry) {
                        JsonObject obj = adapterTimeseriesCatalogEntry.toJsonTree((TimeseriesCatalogEntry)value.getActualInstance()).getAsJsonObject();
                        elementAdapter.write(out, obj);
                        return;
                    }

                    throw new IOException("Failed to serialize as the type doesn't match oneOf schemas: LocationCatalogEntry, TimeseriesCatalogEntry");
                }

                @Override
                public CatalogEntry read(JsonReader in) throws IOException {
                    Object deserialized = null;
                    JsonObject jsonObject = elementAdapter.read(in).getAsJsonObject();

                    int match = 0;
                    ArrayList<String> errorMessages = new ArrayList<>();
                    TypeAdapter actualAdapter = elementAdapter;

                    // deserialize LocationCatalogEntry
                    try {
                        // validate the JSON object to see if any exception is thrown
                        LocationCatalogEntry.validateJsonObject(jsonObject);
                        actualAdapter = adapterLocationCatalogEntry;
                        match++;
                        log.log(Level.FINER, "Input data matches schema 'LocationCatalogEntry'");
                    } catch (Exception e) {
                        // deserialization failed, continue
                        errorMessages.add(String.format("Deserialization for LocationCatalogEntry failed with `%s`.", e.getMessage()));
                        log.log(Level.FINER, "Input data does not match schema 'LocationCatalogEntry'", e);
                    }

                    // deserialize TimeseriesCatalogEntry
                    try {
                        // validate the JSON object to see if any exception is thrown
                        TimeseriesCatalogEntry.validateJsonObject(jsonObject);
                        actualAdapter = adapterTimeseriesCatalogEntry;
                        match++;
                        log.log(Level.FINER, "Input data matches schema 'TimeseriesCatalogEntry'");
                    } catch (Exception e) {
                        // deserialization failed, continue
                        errorMessages.add(String.format("Deserialization for TimeseriesCatalogEntry failed with `%s`.", e.getMessage()));
                        log.log(Level.FINER, "Input data does not match schema 'TimeseriesCatalogEntry'", e);
                    }

                    if (match == 1) {
                        CatalogEntry ret = new CatalogEntry();
                        ret.setActualInstance(actualAdapter.fromJsonTree(jsonObject));
                        return ret;
                    }

                    throw new IOException(String.format("Failed deserialization for CatalogEntry: %d classes match result, expected 1. Detailed failure message for oneOf schemas: %s. JSON: %s", match, errorMessages, jsonObject.toString()));
                }
            }.nullSafe();
        }
    }

    // store a list of schema names defined in oneOf
    public static final Map<String, GenericType> schemas = new HashMap<String, GenericType>();

    public CatalogEntry() {
        super("oneOf", Boolean.FALSE);
    }

    public CatalogEntry(LocationCatalogEntry o) {
        super("oneOf", Boolean.FALSE);
        setActualInstance(o);
    }

    public CatalogEntry(TimeseriesCatalogEntry o) {
        super("oneOf", Boolean.FALSE);
        setActualInstance(o);
    }

    static {
        schemas.put("LocationCatalogEntry", new GenericType<LocationCatalogEntry>() {
        });
        schemas.put("TimeseriesCatalogEntry", new GenericType<TimeseriesCatalogEntry>() {
        });
    }

    @Override
    public Map<String, GenericType> getSchemas() {
        return CatalogEntry.schemas;
    }

    /**
     * Set the instance that matches the oneOf child schema, check
     * the instance parameter is valid against the oneOf child schemas:
     * LocationCatalogEntry, TimeseriesCatalogEntry
     *
     * It could be an instance of the 'oneOf' schemas.
     * The oneOf child schemas may themselves be a composed schema (allOf, anyOf, oneOf).
     */
    @Override
    public void setActualInstance(Object instance) {
        if (instance instanceof LocationCatalogEntry) {
            super.setActualInstance(instance);
            return;
        }

        if (instance instanceof TimeseriesCatalogEntry) {
            super.setActualInstance(instance);
            return;
        }

        throw new RuntimeException("Invalid instance type. Must be LocationCatalogEntry, TimeseriesCatalogEntry");
    }

    /**
     * Get the actual instance, which can be the following:
     * LocationCatalogEntry, TimeseriesCatalogEntry
     *
     * @return The actual instance (LocationCatalogEntry, TimeseriesCatalogEntry)
     */
    @Override
    public Object getActualInstance() {
        return super.getActualInstance();
    }

    /**
     * Get the actual instance of `LocationCatalogEntry`. If the actual instance is not `LocationCatalogEntry`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `LocationCatalogEntry`
     * @throws ClassCastException if the instance is not `LocationCatalogEntry`
     */
    public LocationCatalogEntry getLocationCatalogEntry() throws ClassCastException {
        return (LocationCatalogEntry)super.getActualInstance();
    }

    /**
     * Get the actual instance of `TimeseriesCatalogEntry`. If the actual instance is not `TimeseriesCatalogEntry`,
     * the ClassCastException will be thrown.
     *
     * @return The actual instance of `TimeseriesCatalogEntry`
     * @throws ClassCastException if the instance is not `TimeseriesCatalogEntry`
     */
    public TimeseriesCatalogEntry getTimeseriesCatalogEntry() throws ClassCastException {
        return (TimeseriesCatalogEntry)super.getActualInstance();
    }


 /**
  * Validates the JSON Object and throws an exception if issues found
  *
  * @param jsonObj JSON Object
  * @throws IOException if the JSON Object is invalid with respect to CatalogEntry
  */
  public static void validateJsonObject(JsonObject jsonObj) throws IOException {
    // validate oneOf schemas one by one
    int validCount = 0;
    ArrayList<String> errorMessages = new ArrayList<>();
    // validate the json string with LocationCatalogEntry
    try {
      LocationCatalogEntry.validateJsonObject(jsonObj);
      validCount++;
    } catch (Exception e) {
      errorMessages.add(String.format("Deserialization for LocationCatalogEntry failed with `%s`.", e.getMessage()));
      // continue to the next one
    }
    // validate the json string with TimeseriesCatalogEntry
    try {
      TimeseriesCatalogEntry.validateJsonObject(jsonObj);
      validCount++;
    } catch (Exception e) {
      errorMessages.add(String.format("Deserialization for TimeseriesCatalogEntry failed with `%s`.", e.getMessage()));
      // continue to the next one
    }
    if (validCount != 1) {
      throw new IOException(String.format("The JSON string is invalid for CatalogEntry with oneOf schemas: LocationCatalogEntry, TimeseriesCatalogEntry. %d class(es) match the result, expected 1. Detailed failure message for oneOf schemas: %s. JSON: %s", validCount, errorMessages, jsonObj.toString()));
    }
  }

 /**
  * Create an instance of CatalogEntry given an JSON string
  *
  * @param jsonString JSON string
  * @return An instance of CatalogEntry
  * @throws IOException if the JSON string is invalid with respect to CatalogEntry
  */
  public static CatalogEntry fromJson(String jsonString) throws IOException {
    return JSON.getGson().fromJson(jsonString, CatalogEntry.class);
  }

 /**
  * Convert an instance of CatalogEntry to an JSON string
  *
  * @return JSON string
  */
  public String toJson() {
    return JSON.getGson().toJson(this);
  }
}

